%nav#main-nav
  %ul.list-unstyled
    = nav_link_to 'Dashboard', 'dashboard', root_path

  %ul.list-unstyled
    =# link_to users_path, class: 'user-icon' do
      =# image_tag gravatar_image_url(current_user)
    =# nav_link_to 'Sign Out', 'sign-out', destroy_user_session_path, method: :delete, fuzzy: true
